import 'package:flutter/material.dart';

class MooMooBeenzCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Padding(
        padding: EdgeInsets.all(20.0),
        child: Text("MooMooBeenz Card")
      )
    );
  }
}